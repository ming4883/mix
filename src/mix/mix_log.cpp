#include <mix/mix_log.h>

namespace mix
{
#if 0
    void Log::init()
    {
    }

    void Log::shutdown()
    {
    }

    void Log::write (LogLevel::Enum _level, const char* _tag, const char* _str)
    {
    }
#endif
}
